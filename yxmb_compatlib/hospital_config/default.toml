pinyin = ""

# 通用设置
[settings]
  default_timeout = 10
  login_retries = 4 # 登录失败时的重试次数
  login_timeout = 5
  expiration_date = "2099-02-22" # 软件过期日期
  ignore_department = true # 是否忽略科室选择

# 用户凭据（通常由 admin.txt 提供）
[credentials]
  url = ""
  username = ""
  password = ""
  department_name = ""

# 登录页面
[login]
  # 场景一：常见登录页
  username = ["id", "j_username"]
  password = ["id", "j_password"]
  login_button = ["class_name", "submit-btn"]

  # 验证码相关（如果存在）
  captcha_enabled = true # 通过此开关控制是否处理验证码，医院配置可覆盖为 false
  captcha_image = ["id", "img"]
  captcha_input = ["xpath", "//*[@id='verifyCode']"]

  # 登录后选择科室（动态XPath，使用 .format(科室名) 填充）
  department_button = ["xpath", "//div[contains(text(), '{name}')]"]

  # 登录后执行的操作序列
  # type 可为: "switch_window", "maximize_window", "click", "handle_alert"
  post_login_actions = [
    { type = "switch_window" },
    { type = "maximize_window" },
    { type = "handle_alert" },
    { type = "click", locator = ["xpath", "//*[@id='dd65-ddheader']"], description = "点击主菜单/导航头" },
    { type = "handle_alert" },
    { type = "click", locator = ["xpath", "//span[text()='健康档案']"], description = "点击健康档案菜单" },
    { type = "handle_alert" },
    { type = "click", locator = ["xpath", "//*[@id='button-1005-btnIconEl']"], description = "关闭可能出现的弹窗", optional = true }
  ]

# 引入用药
[medicine]
  name_similarity_threshold = 0.8 # 药品名称相似度阈值

# 数据验证配置
[validation]
  # 定义用于查找日期的列名，按优先级顺序排列
  date_columns = ["随访日期", "成功", "日期"]

  # 是否启用身份证严格校验
  strict_id_check = true

# 菜单配置
[menus]
  # 定义菜单ID映射
  "体检服务" = "menu_5_1"
  "慢病随访" = "menu_3_3"
  "普通随访" = "menu_3_1"

# 列名别名配置
# key 是程序内部使用的标准名，value 是 Excel 中可能出现的列名列表
[column_aliases]
  follow_way = ["随访方式"]
  is_national_standard = ["是否加入国家标准版"]
  compliance = ["服药依从性"]
  untoward_effect = ["药物不良反应"]
  untoward_effect_desc = ["药物不良反应描述", "药物不良反应编辑框"]
  followup_category_bp = ["血压随访分类", "此次随访分类"]
  followup_category_bg = ["血糖随访分类", "此次随访分类"]
  hypoglycemia = ["低血糖反应"]
  symptom = ["症状"]
  symptom_desc = ["症状编辑框"]
  height = ["身高"]
  sbp = ["收缩压", "高压"]
  dbp = ["舒张压", "低压"]
  weight_cur = ["体重", "体重当前", "当前体重"]
  weight_tar = ["目标体重"]
  heart_rate_cur = ["心率", "心率当前", "当前心率"]
  heart_rate_tar = ["目标心率"]
  dorsalis_pedis_normal = ["足背动脉搏动触及正常"]
  dorsalis_pedis_weaken = ["足背动脉搏动减弱"]
  dorsalis_pedis_disappear = ["足背动脉搏动消失"]
  other_sign = ["其他体征"]
  sm_amount_cur = ["日吸烟量", "日吸烟量当前"]
  sm_amount_tar = ["目标日吸烟量"]
  dk_amount_cur = ["日饮酒量", "日饮酒量当前"]
  dk_amount_tar = ["目标日饮酒量"]
  ex_cycle_cur = ["运动次数", "运动频率当前"]
  ex_cycle_tar = ["目标运动次数", "运动频率目标"]
  ex_time_cur = ["运动时间", "运动时间当前"]
  ex_time_tar = ["目标运动时间"]
  st_amount_cur = ["摄盐情况当前"]
  st_amount_tar = ["摄盐情况目标"]
  fh_amount_cur = ["主食量", "主食情况当前"]
  fh_amount_tar = ["目标主食量"]
  psychological = ["心理调整"]
  follow_behavior = ["遵医行为"]
  fbg = ["空腹血糖"]
  pbg = ["餐后血糖"]
  hba1c = ["糖化血红蛋白"]
  medication_info = ["用药情况"]
  referral_reason = ["转诊原因"]
  referral_org_dept = ["机构及科别"]
  assess = ["评估"]
  lifestyle = ["生活方式"]
  suggest = ["医生建议"]
  tc = ["TC", "总胆固醇"]
  tg = ["TG", "甘油三酯"]
  ldl_c = ["LDL-c", "低密度脂蛋白胆固醇"]
  hdl_c = ["HDL-c", "高密度脂蛋白胆固醇"]
  bun = ["BUN", "尿素氮"]
  cr = ["Cr", "肌酐"]
  ccr = ["肌酐清除率"]
  uran = ["尿检"]
  malb = ["尿微量白蛋白"]
  ecg = ["心电图"]
  fundus_oculi = ["眼底"]
  other_test = ["其他辅助检查"]
  is_diabetes = ["是否糖尿病"]
  is_elderly = ["是否老年人"]
  is_cough = ["是否咳嗽、咳痰≥2周"]
  is_hemoptysis = ["是否痰中带血或咯血"]
